const section = document.querySelectorAll(".animation");
const links = document.querySelectorAll('.header-link')    

const sectionUpServer = new IntersectionObserver((entries) => {
console.log(sectionUpServer);
    entries.forEach(links.IntersectionObserver =>) {
    links.forEach(link){
        console.log(link);
        
    } 
}
}, {})



    section.forEach(item => sectionUpServer.observe(item))